div(class="userinfo-container input-container")
  .duplicate-wrap
    label.input-label
      input(type="email" id="email" name="email" class="signin-input email-input")
      p(class="input-label-p") 이메일
      button(class="input-erase-btn"): i.fas.fa-times
      i.fas.fa-check.check-icon
    button.confirm-duplicate 중복확인
  label.input-label
    input(type="text" id="nickname" name="nickname" class="signin-input nickname-input")
    p(class="input-label-p") 닉네임
    button(class="input-erase-btn"): i.fas.fa-times
    i.fas.fa-check.check-icon
  label.input-label
    input(type="password" id="pw" name="pw" class="signin-input pw-input")
    p(class="input-label-p") 비밀번호
    button(class="input-erase-btn"): i.fas.fa-times
    i.fas.fa-check.check-icon
  label.input-label
    input(type="number" id="brith" name="brith" class="signin-input brith-input" placeholder="예) 2000.01.01")
    p(class="input-label-p") 생년월일
    button(class="input-erase-btn"): i.fas.fa-times
    i.fas.fa-check.check-icon
