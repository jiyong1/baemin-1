//- 약관동의 컨테이너
.clause-container#clause
  .clauseTitle 
    | 어서오세요
    br
    | 약관동의가 필요해요
  .allCheckWrap
    input#allCheck(name='allCheck' type='checkbox')
    label(for='allCheck')
    b 전체동의
  ul.checkLists
    -const checkLists = [{id: 'check_1', text: '배달의민족 이용약관 동의', required : true},{id: 'check_2', text: '전자금융거래 이용약관 동의', required : true},{id: 'check_3', text: '개인정보 수집이용 동의', required : true},{id: 'check_4', text: '개인정보 제3자 제공 동의 (선택)'},{id: 'check_5', text: '마케팅 정보 메일, SMS 수신동의 (선택)', noArrow:true}]
    each item in checkLists
      li(class= item.noArrow ? "": "rightArrow")
        input(id=item.id name=item.id type='checkbox' class=item.required ? 'checkItem required' : 'checkItem')
        label(for=item.id)
        .doc= item.text
  .radioWrap 
    label(for="over") 
      input(type="radio" name='overFourteen' id="over" value='over' checked)
      .redioImg
      .radioTitle 만 14세 이상입니다.
    
      img.radioImg(src='/images/clause-radio-1.png' alt='라디오버튼이미지')
    label(for="nonOver")
      input(type="radio" name='overFourteen' id="nonOver" value='nonOver')
      .redioImg
      .radioTitle 만 14세 미만입니다.
        .subText 본인확인이 된 보호자의 배민계정이 필요해요.
      img.radioImg(src='/images/clause-radio-2.png' alt='라디오버튼이미지')
    button#clauseNextBtn.nextBtn(disabled) 다음으로